stargazers:
  target: dev
  outputs:
    dev:
      # type: postgres
      # threads: 1
      # host: localhost
      # port: 5432
      # user: postgres
      # pass: password
      # dbname: postgres
      # schema: public
      type: duckdb
      path: /tmp/airbyte_local/opendatastack-in-a-box.duckdb 
      schema: main
